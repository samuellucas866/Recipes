[pytest]

DJANGO_SETTING_MODULE = project.settings
python_files = test.py tests.py test_*.py tests_*.py *_tests.py *_tests.py
addopts = 
 --doctest-modules
 --strict-markers

markers = 
 slow: Run tests that are slow
 fast: Run fast tests